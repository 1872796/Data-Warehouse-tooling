{
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "env": {
    "REACT_APP_ENV": "production"
  },
  "routes": [
    {
      "src": "/[^.]+$",
      "destination": "/index.html"
    }
  ]
}
